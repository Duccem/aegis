import { Button } from "../ui/button";

const Header = () => {
  return (
    <header className="bg-background top-0 flex shrink-0 items-center justify-end gap-2  p-4">
      <Button>Chat</Button>
    </header>
  );
};

export default Header;
